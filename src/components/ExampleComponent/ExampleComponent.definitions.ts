/**
 * ExampleComponentProps
 *
 * @property {number} startingNumber The number to increment the number by. Optional. Default is 0.
 */
export interface ExampleComponentProps {
  incrementAmount?: number;
}
