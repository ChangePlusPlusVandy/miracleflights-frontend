/* eslint-disable autofix/no-unused-vars */
export interface OnboardingInput {
  dob: string;
  firstName: string;
  lastName: string;
}

export enum OnboardingInputName {
  DOB = "dob",
  FIRST_NAME = "firstName",
  LAST_NAME = "lastName",
}
